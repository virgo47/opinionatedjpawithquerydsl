To start H2 and init the DB with provided shell scripts you have to have `java` and `groovy`
command on your `PATH` environment variable.

Then you just run (parameter is the name of directory with create/init db scrips):
```
$ ./start-h2.sh 00-basic
```